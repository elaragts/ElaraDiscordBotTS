import {SongBestData, UserData} from "./taiko";

export type LeaderboardEntry = {
    my_don_name: string,
    baid: number,
    best_score: number,
    best_crown: number,
    best_score_rank: number
}
